// Previamente ya se selecciono un registro de la tabla, y se copia el campo usuario, que se pasa al método.
// DEPENDENCIA: Debe existir un método que determine si se seleccionó el registro, si no envía un mensaje.

eliminaUsuario(usuario){
	// Se construye el query con el id del usuario pasado como parámetro
	// Se muestra una ventana de confirmación, con la información del usuario a eliminar. 
	// CANCELADO

	if(clickEnConfirmar)
}