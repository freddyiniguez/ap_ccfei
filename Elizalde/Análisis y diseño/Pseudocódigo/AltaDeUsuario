//verificaCampos
// muestraNuevoUsuarioen la tabla
// validaPassword & validaEmail - De la vista.

// El método que valida que los password coincidan y que el formato del
// correo sea válido es el validaFrm()

validaFormUsuarios(verificaCampos(usuario, nombres, apellidpPaternom apellidoMaterno, email, password, rol)){
	// Construyes el query
	query;
	// Realizas el insert
	if(inserta(query))
		return 0;
		// El usuario ha sido agregado correctamente
		// Realizar actualización/refresco de la vista de la BD
	else
		return 2;
		// La BD no está disponible en este momento.
}else{
	return 1;
	// Hay campos vacíos.
}